
module.exports = function (options) {
    const option = {
        type: 'rtu',
    }
    Object.assign(option, options);

    switch (options.type) {
        case 'rtu':
        case 'RTU':

            break;
    }


}

